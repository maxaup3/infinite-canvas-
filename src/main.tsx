import React from 'react'
import ReactDOM from 'react-dom/client'
import TldrawPocApp from './TldrawPocApp'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <TldrawPocApp />
  </React.StrictMode>,
)
